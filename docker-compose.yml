version: '3.1'

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 27050:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: MagicAtlas
      MONGO_INITDB_ROOT_PASSWORD: hhy172hhy